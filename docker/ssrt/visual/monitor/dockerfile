# 基础镜像
FROM  openjdk:8-jre
# author
MAINTAINER ssrt

# 挂载目录
VOLUME /home/ssrt
# 创建目录
RUN mkdir -p /home/ssrt
# 指定路径
WORKDIR /home/ssrt
# 复制jar文件到路径
COPY ./jar/ssrt-visual-monitor.jar /home/ssrt/ssrt-visual-monitor.jar
# 启动系统服务
ENTRYPOINT ["java","-jar","ssrt-visual-monitor.jar"]